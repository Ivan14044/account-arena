# 🛍️ Руководство по использованию магазина аккаунтов

Проект успешно переделан в магазин по продаже аккаунтов!

## ✅ Что было сделано

### Backend (Laravel)

1. **Добавлены новые поля в таблицу `service_accounts`**:
   - `price` - цена товара
   - `title` - название товара (для клиента)
   - `description` - описание товара

2. **Обновлен контроллер** `ServiceAccountController`:
   - Добавлена валидация для новых полей
   - Обновлены методы создания и редактирования

3. **Создан новый API контроллер** `AccountController`:
   - `GET /api/accounts` - получить список всех активных аккаунтов
   - `GET /api/accounts/{id}` - получить детали конкретного аккаунта

4. **Обновлена админ-панель**:
   - Формы создания/редактирования теперь включают поля: Title, Price, Description
   - Список товаров показывает цену и название
   - Упрощен интерфейс управления товарами

### Frontend (Vue.js)

1. **Создан новый компонент** `AccountSection.vue`:
   - Отображает каталог товаров в виде карточек
   - Показывает цену, название, описание и логотип сервиса
   - Кнопка "Купить" для каждого товара

2. **Добавлена секция на главную страницу**:
   - Новая секция "Магазин аккаунтов" между hero и services
   - Автоматически загружает товары из API
   - Адаптивная сетка 1-3 колонки

3. **Добавлены переводы**:
   - Русский и английский языки
   - Тексты для интерфейса магазина

## 📋 Как использовать

### Для администратора

#### 1. Добавить товар в магазин

1. Войдите в админ-панель: http://localhost:8000/admin
2. Перейдите в "Service accounts"
3. Нажмите "+ Add"
4. Заполните форму:
   - **Service** - выберите тип аккаунта (ChatGPT, Canva и т.д.)
   - **Title** - название для покупателей (например, "ChatGPT Premium Account")
   - **Price** - цена в долларах
   - **Description** - описание товара
   - **Status** - Active/Inactive
   - **Expiring at** - срок действия (опционально)
   - **Credentials** - email и password аккаунта

5. Нажмите "Create"

#### 2. Управление товарами

- **Редактирование**: Нажмите иконку карандаша у товара
- **Удаление**: Нажмите иконку корзины
- **Фильтрация**: Товары сортируются по дате создания (новые первыми)

### Для посетителей сайта

1. Откройте сайт: http://localhost:3000
2. Прокрутите до секции "Магазин аккаунтов"
3. Просмотрите доступные товары:
   - Карточка показывает логотип сервиса
   - Название и описание
   - Цену
4. Нажмите "Купить" для просмотра деталей

## 🔧 API Endpoints

### Получить список товаров
```http
GET /api/accounts
```

**Response:**
```json
[
  {
    "id": 1,
    "title": "ChatGPT Premium Account",
    "description": "Full access to GPT-4 and premium features",
    "price": 29.99,
    "service": {
      "id": 1,
      "name": "ChatGPT",
      "code": "chatgpt",
      "logo": "/img/no-logo.png"
    },
    "created_at": "2025-10-31T10:00:00.000000Z"
  }
]
```

### Получить товар по ID
```http
GET /api/accounts/1
```

**Response:**
```json
{
  "id": 1,
  "title": "ChatGPT Premium Account",
  "description": "Full access to GPT-4 and premium features",
  "price": 29.99,
  "service": {
    "id": 1,
    "name": "ChatGPT",
    "code": "chatgpt",
    "logo": "/img/no-logo.png",
    "full_description": "Advanced AI language model..."
  },
  "created_at": "2025-10-31T10:00:00.000000Z"
}
```

## 📊 Структура данных

### ServiceAccount (service_accounts таблица)

| Поле | Тип | Описание |
|------|-----|----------|
| id | bigint | ID аккаунта |
| service_id | bigint | Ссылка на сервис |
| profile_id | string | ID профиля браузера (для KASM) |
| credentials | json | Email и password |
| is_active | boolean | Активен ли товар |
| used | integer | Счетчик использования |
| expiring_at | timestamp | Дата истечения |
| last_used_at | timestamp | Последнее использование |
| **price** | decimal | **Цена товара** |
| **title** | string | **Название для покупателей** |
| **description** | text | **Описание товара** |
| created_at | timestamp | Дата создания |
| updated_at | timestamp | Дата обновления |

## 🎨 Дальнейшие улучшения

1. **Страница детального просмотра товара**
   - Создать отдельную страницу `/account/:id`
   - Показать полную информацию и характеристики
   - Форма покупки

2. **Корзина покупок**
   - Добавить товары в корзину
   - Оформление заказа
   - Интеграция с платежной системой

3. **Фильтры и поиск**
   - Фильтр по цене
   - Поиск по названию
   - Сортировка

4. **Система продаж**
   - Автоматическая выдача credentials после оплаты
   - Email с данными аккаунта
   - История покупок

## 🔐 Безопасность

⚠️ **Важно**: Credentials хранятся в открытом виде в БД. Для продакшена рекомендую:

1. Использовать шифрование для credentials
2. Выдавать доступ к товару только после оплаты
3. Ограничить доступ к API для неавторизованных пользователей
4. Добавить rate limiting для API

## 📝 Примеры использования

### Добавить тестовый товар через Laravel Tinker

```bash
cd backend
php artisan tinker
```

```php
$service = App\Models\Service::first();

App\Models\ServiceAccount::create([
    'service_id' => $service->id,
    'title' => 'ChatGPT Plus Account',
    'description' => 'Premium account with GPT-4 access',
    'price' => 25.99,
    'credentials' => [
        'email' => 'test@example.com',
        'password' => 'securepassword123'
    ],
    'is_active' => true,
]);
```

## 🆘 Решение проблем

### Товары не отображаются на сайте

1. Проверьте, что товары созданы через админ-панель
2. Убедитесь, что `is_active = true` и `used = 0`
3. Проверьте API: `GET http://localhost:8000/api/accounts`
4. Откройте консоль браузера (F12) для просмотра ошибок

### Ошибки при создании товара

1. Проверьте, что все обязательные поля заполнены
2. Цена должна быть числом (с запятой или точкой)
3. Проверьте логи: `backend/storage/logs/laravel.log`

### База данных

Если миграции не применены:
```bash
cd backend
php artisan migrate
```

## 📚 Дополнительная информация

- Админ-панель: http://localhost:8000/admin
- API документация: http://localhost:8000/api/accounts
- Frontend: http://localhost:3000

---

Проект готов к использованию! 🎉

