# Инструкция по перезапуску

## Проблема
Frontend подключился к производственному API вместо локального.

## Решение - ПЕРЕЗАПУСТИТЬ FRONTEND

Я создал файл `frontend/.env` с правильной настройкой:
```
VITE_API_BASE=http://localhost:8000
```

Но Vite загружает .env файлы только при старте, поэтому нужно перезапустить!

### Шаг 1: Найдите окно PowerShell с фронтендом
Откройте окна PowerShell - одно из них запускает frontend.

### Шаг 2: Остановите фронтенд
Нажмите `Ctrl+C` в окне где запущен frontend (порт 3000).

### Шаг 3: Перезапустите
В том же окне выполните:
```
cd frontend
npm run dev -- --port 3000
```

### ИЛИ перезапустите весь проект:
Закройте все PowerShell окна с серверами и выполните:
```
.\start-dev.ps1
```

После перезапуска сайт должен работать! 🎉

## Альтернатива (если не помогает)
Откройте консоль браузера (F12) и посмотрите ошибки. Возможно, проблема в другом.

