<?php

/*
 * Overrides for the jeroennoten/laravel-adminlte package strings.
 *
 * The application runs with locale "en" while the whole panel UI is in Russian,
 * so a few package-provided navbar strings (e.g. the logout link) would
 * otherwise render in English. Only the keys present here override the package
 * defaults; all other keys fall back to the package file.
 */

return [
    'log_out' => 'Выйти',
];
