# 🚀 Быстрый старт - AI Bot

Проект успешно собран и запущен! 🎉

## ✅ Что готово

- ✅ Backend (Laravel) - http://localhost:8000
- ✅ Frontend (Vue.js) - http://localhost:3000
- ✅ Зависимости установлены
- ✅ Ключи сгенерированы
- ✅ CORS настроен
- ✅ Storage линк создан

## 📝 Следующие шаги

### 1. Настройка базы данных

Вам нужно настроить базу данных MySQL. Отредактируйте `backend/.env`:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=ai_bot
DB_USERNAME=root
DB_PASSWORD=ваш_пароль
```

Затем создайте базу данных и выполните миграции:

```bash
# Создайте базу данных в MySQL
CREATE DATABASE ai_bot CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# Выполните миграции
cd backend
php artisan migrate
php artisan db:seed
```

### 2. Настройка дополнительных сервисов (опционально)

В `backend/.env` есть настройки для:
- Google OAuth
- Telegram Bot
- Cryptomus (крипто платежи)
- Monobank

Заполните их при необходимости.

### 3. Админ панель

Похоже, что у проекта есть админ панель на Laravel AdminLTE. Проверьте:
- http://localhost:8000/admin

## 🎯 Запуск проекта

### Простой способ (PowerShell):
```powershell
.\start-dev.ps1
```

### Или вручную в двух терминалах:

**Терминал 1 - Backend:**
```bash
cd backend
php artisan serve --port=8000
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev -- --port=3000
```

## 📁 Структура проекта

```
ai-bot-monorepo/
├── backend/          # Laravel API
│   ├── app/          # Приложение
│   ├── config/       # Конфигурация
│   ├── database/     # Миграции и сидеры
│   ├── routes/       # API роуты
│   ├── .env          # Настройки окружения
│   └── composer.json
├── frontend/         # Vue.js приложение
│   ├── src/
│   ├── .env          # API URL
│   └── package.json
├── package.json      # Корневые скрипты
├── README.md         # Полная документация
└── start-dev.ps1     # Скрипт запуска
```

## 🔧 Полезные команды

```bash
# Установить все зависимости
npm run setup

# Запустить backend
npm run start:backend

# Запустить frontend
npm run start:frontend

# Собрать frontend для production
npm run build:frontend

# Линтинг
npm run lint:frontend
```

## ⚠️ Важные замечания

1. **База данных**: Проект не будет работать без настроенной БД
2. **Порты**: Убедитесь, что порты 8000 и 3000 свободны
3. **PHP**: Требуется PHP 8.1+
4. **Node.js**: Требуется Node.js 18+

## 🐛 Решение проблем

### Порт занят?
Измените порт в команде запуска или в `.env`

### CORS ошибки?
Backend уже настроен, но если что-то не работает, проверьте `backend/config/cors.php`

### Ошибки подключения к БД?
Убедитесь, что MySQL запущен и данные в `.env` корректны

## 📚 Документация

- Полная документация в `README.md`
- Laravel: https://laravel.com/docs
- Vue.js: https://vuejs.org/docs

Удачи в разработке! 🚀

