# Account Arena - Frontend (Vue 3)

Frontend часть проекта Account Arena на базе Vue 3 с TypeScript.

## 🛠 Технологии

- **Vue 3.5** - Progressive JavaScript Framework
- **TypeScript 5.9** - Статическая типизация
- **Vite 5.4** - Современный сборщик
- **Vue Router 4** - Маршрутизация
- **Pinia 3** - State management
- **Vuetify 3** - Material Design компоненты
- **Tailwind CSS 3** - Utility-first CSS
- **Vue I18n 11** - Интернационализация
- **Axios** - HTTP клиент
- **Swiper** - Слайдеры и карусели
- **Lottie** - Анимации

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── assets/                  # Статические ресурсы
│   │   ├── img/                 # Изображения
│   │   ├── payment-icons/       # Иконки платежных систем
│   │   └── *.json               # JSON файлы (анимации, темы)
│   ├── components/               # Vue компоненты
│   │   ├── auth/                # Компоненты авторизации
│   │   ├── checkout/            # Компоненты оформления заказа
│   │   ├── home/                # Компоненты главной страницы
│   │   ├── layout/              # Компоненты layout
│   │   └── products/            # Компоненты товаров
│   ├── composables/            # Composition API функции
│   │   ├── useArticle.ts        # Работа со статьями
│   │   ├── useBanners.ts        # Работа с баннерами
│   │   ├── useProductTitle.ts   # Генерация заголовков
│   │   ├── useServiceLauncher.ts # Запуск сервисов
│   │   └── useTheme.ts          # Управление темой
│   ├── directives/              # Кастомные директивы
│   │   └── intersect.ts         # Intersection Observer
│   ├── i18n/                    # Интернационализация
│   │   ├── index.js             # Конфигурация i18n
│   │   └── locales/             # Переводы (ru, en, uk)
│   ├── pages/                   # Страницы приложения
│   │   ├── account/             # Страницы аккаунта
│   │   ├── articles/            # Страницы статей
│   │   └── *.vue                # Основные страницы
│   ├── plugins/                 # Vue плагины
│   │   └── vuetify.js           # Конфигурация Vuetify
│   ├── router.js                # Конфигурация Vue Router
│   ├── stores/                  # Pinia stores
│   │   ├── accounts.ts          # Хранилище товаров
│   │   ├── articles.ts          # Хранилище статей
│   │   ├── auth.ts             # Хранилище авторизации
│   │   ├── banners.ts           # Хранилище баннеров
│   │   ├── productCart.ts       # Корзина
│   │   └── ...                  # Другие stores
│   ├── types/                   # TypeScript типы
│   │   ├── article.ts           # Типы для статей
│   │   └── telegram.d.ts        # Типы для Telegram
│   ├── utils/                   # Утилиты
│   │   ├── alert.js             # Уведомления
│   │   ├── performance.ts       # Оптимизация производительности
│   │   ├── pluralize.ts         # Плюрализация
│   │   └── scrollToElement.ts   # Прокрутка к элементу
│   ├── bootstrap.js             # Инициализация приложения
│   └── main.js                   # Точка входа
├── public/                       # Статические файлы
├── dist/                         # Собранные файлы (после build)
├── package.json                  # Зависимости
├── vite.config.js                # Конфигурация Vite
├── tailwind.config.js            # Конфигурация Tailwind
└── tsconfig.json                 # Конфигурация TypeScript
```

## 🚀 Установка

### Требования

- Node.js >= 18.x
- NPM >= 9.x (или Yarn >= 1.22)

### Шаги установки

```bash
# Установка зависимостей
npm install

# Создание конфигурации
cp .env.example .env

# Настройка .env файла
VITE_API_URL=http://localhost:8000/api

# Запуск dev-сервера
npm run dev
```

Frontend будет доступен на `http://localhost:3000` (или другой порт, указанный Vite)

## 📜 Доступные команды

```bash
# Разработка
npm run dev              # Запуск dev-сервера с hot reload

# Сборка
npm run build            # Production сборка
npm run preview          # Предпросмотр production сборки

# Линтинг и форматирование
npm run lint             # Проверка кода ESLint
npm run lint:fix         # Автоматическое исправление ошибок
npm run format           # Форматирование кода Prettier

# TypeScript
npm run type-check       # Проверка типов TypeScript
```

## ⚙️ Конфигурация

### Переменные окружения (.env)

```env
# API endpoint
VITE_API_URL=http://localhost:8000/api
```

> **Примечание:** Frontend поддерживает обе переменные `VITE_API_URL` и `VITE_API_BASE`. Рекомендуется использовать `VITE_API_URL`. Если заданы обе, приоритет у `VITE_API_URL`.

### Vite конфигурация

Основные настройки в `vite.config.js`:
- Алиасы путей (`@/*` → `src/*`)
- Плагины (Vue, I18n)
- Оптимизация сборки

### Tailwind CSS

Конфигурация в `tailwind.config.js`:
- Кастомные цвета
- Расширенные утилиты
- Плагины (line-clamp)

## 🎨 Компоненты

### Основные компоненты

- **App.vue** - Корневой компонент
- **MainMenu.vue** - Главное меню
- **MobileMenu.vue** - Мобильное меню
- **SupportChatWidget.vue** - Виджет чата поддержки
- **CookieBanner.vue** - Баннер согласия на cookies
- **FullPageLoader.vue** - Полноэкранный загрузчик

### Layout компоненты

- **MainHeader.vue** - Шапка сайта
- **MainFooter.vue** - Подвал сайта
- **AccountSection.vue** - Секция аккаунта

### Компоненты товаров

- **SimilarProducts.vue** - Похожие товары

## 📦 Pinia Stores

### Основные stores

- **auth.ts** - Авторизация и профиль пользователя
- **accounts.ts** - Каталог товаров
- **productCart.ts** - Корзина покупок
- **articles.ts** - Статьи
- **banners.ts** - Баннеры
- **notifications.js** - Уведомления
- **productCategories.ts** - Категории товаров
- **promo.ts** - Промокоды

## 🌍 Интернационализация

Поддерживаемые языки:
- **ru** - Русский (по умолчанию)
- **en** - English
- **uk** - Українська

Файлы переводов находятся в `src/i18n/locales/`.

## 🎯 Основные страницы

- **MainPage.vue** - Главная страница
- **CheckoutPage.vue** - Оформление заказа
- **BalanceTopUpPage.vue** - Пополнение баланса
- **BecomeSupplierPage.vue** - Стать поставщиком
- **OrderSuccessPage.vue** - Успешный заказ
- **ContentPage.vue** - Статические страницы
- **articles/** - Страницы статей
- **account/** - Страницы аккаунта

## 🔌 API интеграция

Все API запросы выполняются через Axios. Базовый URL настраивается через `VITE_API_URL`.

Пример использования:

```typescript
import axios from 'axios'

const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL
})

// С токеном авторизации
api.defaults.headers.common['Authorization'] = `Bearer ${token}`
```

## 🎨 Стилизация

Проект использует комбинацию:
- **Tailwind CSS** - Utility-first стили
- **Vuetify 3** - Material Design компоненты
- **Кастомные стили** - В компонентах через `<style scoped>`

## 🚀 Production сборка

```bash
# Создать production сборку
npm run build

# Файлы будут в директории dist/
```

После сборки файлы из `dist/` должны быть размещены на веб-сервере (Nginx).

## 📝 Разработка

### Создание нового компонента

```bash
# Создайте файл в src/components/
# Пример: src/components/MyComponent.vue
```

### Создание новой страницы

```bash
# Создайте файл в src/pages/
# Добавьте маршрут в src/router.js
```

### Создание нового store

```bash
# Создайте файл в src/stores/
# Пример: src/stores/myStore.ts
```

## 🧪 Тестирование

```bash
# Проверка типов
npm run type-check

# Линтинг
npm run lint
```

## 📚 Дополнительная документация

- [Vue 3 Documentation](https://vuejs.org/)
- [Vite Documentation](https://vitejs.dev/)
- [Pinia Documentation](https://pinia.vuejs.org/)
- [Vuetify 3 Documentation](https://vuetifyjs.com/)
- [Vue Router 4](https://router.vuejs.org/)
- [Vue I18n](https://vue-i18n.intlify.dev/)

## 👨‍💻 Автор

**Ivan Knysh** - [GitHub](https://github.com/Ivan14044)
